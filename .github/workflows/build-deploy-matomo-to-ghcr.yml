name: Build & Test & Push Matomo Image (GHCR)

permissions:
  contents: read
  packages: write

on:
  workflow_dispatch:
    inputs:
      matomo_version:
        description: "Matomo version (e.g. 5.6.1)"
        required: true
        default: "5.6.1"
      php_memory_limit:
        description: "PHP memory_limit (e.g. 256M)"
        required: true
        default: "256M"

env:
  IMAGE_NAME: matomo-appservice-rebuild

jobs:
  # =========================
  # 1) ビルド & テスト
  # =========================
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # .env を生成（ローカル docker-compose と同じ形式）
      - name: Generate .env file
        run: |
          cat <<EOF > .env
          MATOMO_VERSION=${{ inputs.matomo_version }}
          PHP_MEMORY_LIMIT=${{ inputs.php_memory_limit }}
          EOF
          echo ".env generated:"
          cat .env

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # テスト用イメージのビルド（push しない / runner に load）
      - name: Build image for test (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          build-args: |
            MATOMO_VERSION=${{ inputs.matomo_version }}
            PHP_MEMORY_LIMIT=${{ inputs.php_memory_limit }}
          tags: |
            matomo-test:${{ github.sha }}

      # 簡易 PHP 拡張チェック
      - name: Simple PHP extension check
        run: |
          set -e

          echo "== php -m =="
          docker run --rm matomo-test:${{ github.sha }} php -m

          echo "== check required extensions =="
          for ext in gd mysqli pdo_mysql bcmath zip redis apcu; do
            echo "Checking extension: $ext"
            if ! docker run --rm matomo-test:${{ github.sha }} php -m | grep -q "$ext"; then
              echo "ERROR: PHP extension '$ext' not found."
              exit 1
            fi
          done

      # Apache 設定ダンプ（デバッグ用：実際に有効な設定を確認）
      - name: Dump Apache effective config
        run: |
          echo "===== apache2ctl -t (syntax check) ====="
          docker run --rm matomo-test:${{ github.sha }} apache2ctl -t 2>&1 || true
          echo "===== apache2ctl -t -D DUMP_CONFIG ====="
          docker run --rm matomo-test:${{ github.sha }} apache2ctl -t -D DUMP_CONFIG 2>&1 | head -300 || true
          echo "===== /etc/apache2/sites-enabled/ ====="
          docker run --rm matomo-test:${{ github.sha }} ls -la /etc/apache2/sites-enabled/ || true
          echo "===== /etc/apache2/conf-enabled/ ====="
          docker run --rm matomo-test:${{ github.sha }} ls -la /etc/apache2/conf-enabled/ || true
          echo "===== 000-default.conf ====="
          docker run --rm matomo-test:${{ github.sha }} cat /etc/apache2/sites-available/000-default.conf || true
          echo "===== matomo-src.conf ====="
          docker run --rm matomo-test:${{ github.sha }} cat /etc/apache2/conf-available/matomo-src.conf || true

      # 超簡易 HTTP スモークテスト
      - name: HTTP smoke test (apache + matomo)
        run: |
          set -e

          docker run -d --name matomo-http-test \
            -p 8080:80 \
            matomo-test:${{ github.sha }}

          echo "Waiting for container to be ready..."
          # Matomo の起動が重ければ増やす
          sleep 40

          echo "===== Container logs (after startup) ====="
          docker logs matomo-http-test 2>&1 || true

          echo "Requesting http://localhost:8080/ ..."
          http_status=$(curl -sS -o /tmp/index.html -w "%{http_code}" http://localhost:8080/ || echo "000")
          echo "HTTP status: $http_status"

          if [ "$http_status" != "200" ]; then
            echo "ERROR: Expected HTTP 200 but got $http_status"
            echo "===== /tmp/index.html (if any) ====="
            cat /tmp/index.html || true
            echo "===== Container logs (full) ====="
            docker logs matomo-http-test 2>&1 || true
            echo "===== /var/www/html directory listing ====="
            docker exec matomo-http-test ls -la /var/www/html/ || true
            echo "===== /usr/src/matomo directory listing ====="
            docker exec matomo-http-test ls -la /usr/src/matomo/ || true
            echo "===== Apache error log ====="
            docker exec matomo-http-test cat /var/log/apache2/error.log 2>/dev/null | tail -50 || true
            docker rm -f matomo-http-test || true
            exit 1
          fi

          # HTML の中に Matomo っぽい文言があるかザックリ確認
          if ! grep -qi "Matomo" /tmp/index.html; then
            echo "WARNING: 'Matomo' keyword not found in index.html"
            # ここで失敗させたければ exit 1 に変えてOK
          fi

          echo "===== /tmp/index.html (head) ====="
          head -n 50 /tmp/index.html || true

          docker rm -f matomo-http-test || true

      # 🔐 Trivy による脆弱性スキャン（HIGH/CRITICAL があれば失敗させる）
      - name: Scan image for vulnerabilities (Trivy)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: matomo-test:${{ github.sha }}
          vuln-type: 'os,library'
          severity: 'HIGH,CRITICAL'
          ignore-unfixed: true
          format: 'table'
          exit-code: '1'   # ← HIGH/CRITICAL が1件でもあればジョブ失敗

  # =========================
  # 2) GHCR へ push
  # =========================
  push:
    runs-on: ubuntu-latest
    needs: build-and-test   # ← テスト + 脆弱性チェック通ったらだけ実行

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # .env を生成（上と同じ）
      - name: Generate .env file
        run: |
          cat <<EOF > .env
          MATOMO_VERSION=${{ inputs.matomo_version }}
          PHP_MEMORY_LIMIT=${{ inputs.php_memory_limit }}
          EOF
          echo ".env generated:"
          cat .env

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # GitHub Container Registry にログイン
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.PUSH_GITHUB_TOKEN }}

      # ビルドして GHCR に push
      - name: Build and push to GHCR
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            MATOMO_VERSION=${{ inputs.matomo_version }}
            PHP_MEMORY_LIMIT=${{ inputs.php_memory_limit }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ inputs.matomo_version }}
            ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:latest
